name: "Restart all environments"
on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  deploy-to-dev-environment:
    strategy:
      matrix:
        target: ['mock', 'dev', "preprod"]
    uses: ./.github/workflows/build_deploy_dev.yml
    with:
      config-file-name: ${{ matrix.target }}
    secrets: inherit

  deploy-to-prod-environment:
    uses: ./.github/workflows/build_deploy_prod.yml
    secrets: inherit
